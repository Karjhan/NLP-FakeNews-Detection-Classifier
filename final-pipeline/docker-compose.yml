services:
  fake-news-api:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: fake-news-api
    ports:
      - "8800:8000"
    environment:
      - DEVICE=cpu
      - TOKENIZERS_PARALLELISM=false
    restart: unless-stopped
